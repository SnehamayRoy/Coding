#include <bits/stdc++.h>
using namespace std;

bool chekCycle(vector<int> adj[], int n)
{
}

int main()
{
    int n;
    cin >> n;
    vector<int> adj[n];
    for (int i = 0; i < n; i++)
    {
        int m;
        cin >> m;
        adj[i].push_back(m);
    }

    return 0;
}